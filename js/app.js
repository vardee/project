
const pointContainer = document.getElementById('point-container');

export const width = 500;
export const height = 500;
pointContainer.style.width = `${width}px`;
pointContainer.style.height = `${height}px`;